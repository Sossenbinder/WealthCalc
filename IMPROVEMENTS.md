# Improvements

One line per dogfooding iteration: the problem found while using the app, the change made, the file.

- A shared scenario link silently showed the defaults instead of the sender's scenario (5.000 €/400 €/6,5 %/25 Jahre opened as 315.124,67 € for the sender, 165.580,87 € for the recipient) — gated the URL-writing effect on `urlApplied` so it can no longer overwrite the incoming query string before it has been read, and pinned the round-trip with tests — `src/app/zinseszins-rechner/ZinseszinsCalculator.tsx`, `tests/finance/scenario-url.test.ts`.
- On a phone the answer sat below the fold at y≈1024 in an 844 px viewport — every mobile visitor, including anyone opening a shared link, landed on six input fields and had to scroll past a 670 px form to reach the number they came for — dropped the wrapping `<section>` so the result, form and table are direct children of the layout container and ordered result-first on small screens, with the desktop two-column grid pinned via explicit `lg:col-start`/`lg:row-start` so it renders unchanged (answer now at y≈330) — `src/app/zinseszins-rechner/ZinseszinsCalculator.tsx`.
- The most common form of the question — "I want 500.000 € in 25 years, what do I have to save each month?" — could only be answered by guessing: the page had no buttons and no target field, and seven manual attempts at the monthly rate still had not converged — added a "Sparrate" mode that takes a Zielkapital and solves for the required contribution by binary-searching the real projection (so the headline, summary and year table always describe one and the same plan), carried in the URL as `mode`/`z` so a solved scenario stays shareable — `src/lib/engine/finance/zinseszins.ts`, `src/app/zinseszins-rechner/scenario-url.ts`, `src/app/zinseszins-rechner/ZinseszinsCalculator.tsx`.
- Clearing a field to retype it left the headline showing a confident euro figure derived from a hidden default the user had never entered and could not see (empty Sparrate still read 165.580,87 € off an unseen 250 €; a blank Anlagedauer still silently used 20 years), and the same fabricated scenario was written into the shareable URL — the result, the summary and the year table are now withheld whenever a field in use is empty or unreadable, replaced by "—" and a message naming the missing fields, and the URL is left at the last complete scenario rather than a made-up one — `src/app/zinseszins-rechner/ZinseszinsCalculator.tsx`.
- Nothing showed the shape of the growth: answering "when does my interest overtake what I paid in?" (year 17 on the defaults) meant reading all 20 table rows and subtracting two columns by eye — added a stacked area chart of Eingezahlt vs. Zinsertrag between the answer and the table, lazy-loaded so it stays out of the prerendered HTML, stacking exactly to the balance the table reports and with animation off so it does not sweep in again on every keystroke — `src/components/GrowthChart.tsx`, `src/app/zinseszins-rechner/ZinseszinsCalculator.tsx`.
- Keyboard focus vanished on tab stops 3 and 10: both toggles (Endkapital/Sparrate and Monatsende/Monatsanfang) are built from `sr-only` radios, so the browser painted its focus ring on a clipped 1×1 px element while the visible label that is the actual control got nothing — a keyboard user crossed two of seven controls blind, including the mode toggle — forwarded the ring to the label with `has-[:focus-visible]` so the focused option carries a 2 px accent ring that the merely-selected option does not — `src/app/zinseszins-rechner/ZinseszinsCalculator.tsx`.
- Sharing a worked-out plan had no affordance at all — `main` contained zero buttons or links, so the reader had to know the query string was meaningful and then extract a 70-character URL from a phone address bar that shows about its first 30 — added a "Link kopieren" button in the answer card that copies the current URL, confirms via an `aria-live` region, clears the confirmation as soon as the scenario changes, and is withheld while the input is incomplete so a stale link cannot be copied — `src/app/zinseszins-rechner/ZinseszinsCalculator.tsx`.
- Typing an Anlagedauer outside the supported range was silently substituted: the field kept showing 999 while the projection ran for 80 years and the headline reported the 80-year figure, a >12× disagreement between the input and the answer with nothing to signal it (0 and negatives were likewise coerced to 1) — the field now rejects anything outside 1–80 through the existing incomplete-input path, with the constraint stated on the field itself, while a shared link carrying an out-of-range year count still clamps so that the field and the result agree instead of erroring for the recipient — `src/app/zinseszins-rechner/ZinseszinsCalculator.tsx`.
- With a Dynamik set, the plan silently committed the user to a much larger payment than they entered — 300 €/month rising 3 % a year reaches 819,53 €/month by year 35, and the only way to discover it was to read the last table row and divide by 12 (which gives 819,50 €, because that column is rounded to whole euros) — the engine now records the contribution actually paid in each year and reports the final one, and the answer card states "Sparrate steigt von … auf … im Monat" whenever a Dynamik is set, in both Endkapital and Sparrate mode — `src/lib/engine/finance/zinseszins.ts`, `src/app/zinseszins-rechner/ZinseszinsCalculator.tsx`.
- The chart added in the earlier iteration put an unnamed `role="application"` region into the tab order (Recharts' accessibility layer), so a keyboard or screen-reader user landed in a nameless application region whose only content was run-together axis digits ("1234...20Jahr045.00090.000..."), while the same data sat properly structured in the table below — disabled the accessibility layer and marked the chart presentational, leaving the table as the accessible form of the data; the tab order is now eleven meaningful stops ending at "Link kopieren", with no focusable node inside the hidden subtree — `src/components/GrowthChart.tsx`.
- A dead or mistyped URL served Next's built-in 404: English headings ("404", "This page could not be found.") on a page declaring `lang="de"` — the only English on a German-only product, and a screen reader would read it with German pronunciation — with zero links in `main` to recover from and the homepage's title still attached — added a German `not-found.tsx` that explains what happened and offers a way onward, driven by the tool registry so it stays correct as calculators are added, and confirmed the static export ships it as `out/404.html` — `src/app/not-found.tsx`.
- At increased text size the page scrolled sideways: the summary's `sm:grid-cols-3` is keyed to viewport width, which does not change when a user enlarges text, so at 200 % each column was squeezed to 127 px while a currency string like "108.957,84 €" needs 202 px and cannot wrap, pushing the page 30 px wider than the viewport (WCAG 1.4.4/1.4.10) — replaced the viewport breakpoint with `auto-fit`/`minmax` so the columns respond to the space actually available, giving 3 columns at 100 %, 2 at 150 % and 1 at 200 % with no horizontal scroll, and leaving 320/390 px phone layouts unchanged — `src/app/zinseszins-rechner/ZinseszinsCalculator.tsx`.
- With large text enabled on a phone, the "Sparrate" option of the mode toggle ran to x=436 on a 390 px viewport — partly off-screen, so the control that switches the calculator into goal-seek mode could not be seen or reliably tapped, and the row was the single largest source of the page's 147 px sideways scroll — gave both toggle rows the same `auto-fit`/`minmax` treatment as the summary grid so the options stack instead of overflowing; they stay two-across at every normal size and stack only when the space genuinely is not there — `src/app/zinseszins-rechner/ZinseszinsCalculator.tsx`.
- At large text sizes the answer headline widened the whole page: a currency amount has no break opportunity — digits cannot wrap and the space before € is non-breaking — so "165.580,87 €" at 72 px demanded 483 px inside a 324 px card and forced 126 px of sideways scrolling on a phone; shrinking it to fit would have needed roughly a 20 px font, smaller than the surrounding body text of someone who had deliberately enlarged it — let the headline scroll inside its own box instead, as the table already does, keeping the text at full requested size and cutting page-level sideways scroll to 5 px, with `leading-tight` added because `text-4xl` pins line-height below the glyph box and a scroll container would otherwise clip descenders on every viewport — `src/app/zinseszins-rechner/ZinseszinsCalculator.tsx`.
- Three fields with a descriptive hint (Rendite, Inflation, Dynamik) turned that hint red on invalid input instead of saying what was wrong — the field was marked `aria-invalid`, painted as an error, and the red text still read "Effektive Jahresrendite nach Kosten.", so a screen reader announced "invalid" and then a sentence about the field's purpose, while fields without a static hint reported the error correctly — separated the two roles in `NumberField` (`hint` describes, `error` replaces it while invalid) and gave all seven call sites a real message, keeping the Anlagedauer field's two distinct errors — `src/components/NumberField.tsx`, `src/app/zinseszins-rechner/ZinseszinsCalculator.tsx`.
- The page still scrolled sideways by 5 px at large text on a phone because the heading "Zinseszins- und Sparplanrechner" is a German compound with no break opportunity at 48 px — added `hyphens-auto`, which the browser applies using the `lang="de"` already on `<html>`, so it hyphenates ("Sparplanrech-ner") instead of overflowing; page-level sideways scroll is now 0 at desktop, phone 320, phone 390 and phone 390 at 200 % text — `src/components/CalculatorShell.tsx`.
- A rate with three decimals was destroyed by its own link: `scenarioToParams` writes `r=6.125` with a dot decimal point, but `scenarioFromParams` read it back through the German *input* parser, where a dot before three digits is a thousands separator — so 6,125 % became 6125 %, clamped to the 100 % ceiling, and a 147.793,92 € plan reopened as 14.894.280.478,86 €, for the author on reload as much as for anyone they sent the link to — query values are now parsed canonically (`Number`, and cents via an exact comma-swap) instead of as German display text, and the editable fields keep the precision entered rather than rounding to two places, so sender and recipient see the same figure — `src/app/zinseszins-rechner/scenario-url.ts`, `src/lib/engine/format.ts`, `src/app/zinseszins-rechner/ZinseszinsCalculator.tsx`.
- The previous iteration's precision fix was incomplete: it swapped a two-place cap for a four-place one rather than removing it, so a rate of 6,12345 % still came back as 6,1235 on reload or for anyone opening the link, computing 147.765,12 € instead of 147.764,20 € — raised the editable-field precision to ten places, which is past any meaningful precision for a percentage while avoiding both extremes (`maximumFractionDigits: 20` surfaces float artefacts such as "0,30000000000000004", and `String(value)` emits exponential notation below 1e-6 that the German parser rejects); a URL-in/URL-out sweep over eight scenarios now round-trips every one unchanged — `src/lib/engine/format.ts`.
- The form accepted values the URL layer silently clamps, so a scenario changed under the user on their own reload: 500 % typed as the return reopened as 100 % (42 Trillionen € became 14,9 Mrd €), and a 500.000.000 € Einmalanlage reopened as 100.000.000 €, moving the answer by 1,5 Mrd € — the form now rejects anything outside the bounds `scenario-url.ts` enforces, through the same incomplete-input path as Anlagedauer and naming the limit ("Zwischen -100 und 100 %.", "Zwischen 0 und 100.000.000 €."), so an out-of-range value never reaches the URL; boundary values (±100 %, 100.000.000 €) remain accepted — `src/app/zinseszins-rechner/ZinseszinsCalculator.tsx`.
- Sparrate mode answered "you need 772,68 € im Monat" and then discarded that number: switching back to Endkapital restored the rate the reader arrived with, so exploring the plan they had just been told to save meant copying the figure across by hand — added a "Mit dieser Rate weiterrechnen" button that hands the solved rate to Endkapital mode (verified to land back on the target: 772,68 € reaches 500.000,83 € over 22 years), shown only where it applies and left explicit rather than firing on every mode switch, which would overwrite the rate the reader came in with — `src/app/zinseszins-rechner/ZinseszinsCalculator.tsx`.
- The calculator could solve for Endkapital and for Sparrate but not for time, so "when do I cross 100.000 €?" — one of the most common forms of the question — meant guessing at the Anlagedauer (four attempts still only bracketed it to a whole year) or scanning 20 table rows by eye — added a third "Dauer" mode that takes a Zielkapital and reports the first year the plan reaches it (12 Jahre for 10.000 € plus 400 €/Monat at 6 %, matching the row the table scan found), scanning years linearly rather than bisecting because the balance is not monotonic in time once the return is negative, and naming the two edge cases outright ("Bereits erreicht", "Nicht erreichbar") — `src/lib/engine/finance/zinseszins.ts`, `src/app/zinseszins-rechner/scenario-url.ts`, `src/app/zinseszins-rechner/ZinseszinsCalculator.tsx`.
- The Dauer mode added in the previous iteration dropped its own answer: it reported "12 Jahre" and then, on switching to Endkapital to explore that plan, restored the 20 years the reader arrived with — the asymmetry with Sparrate mode, which had gained a hand-over button one iteration earlier — added the matching "Mit dieser Dauer weiterrechnen" (12 Jahre carries over and reproduces 103.301,15 €, the very figure Dauer mode reported reaching), shown only when a duration was actually solved, so neither "Bereits erreicht" nor "Nicht erreichbar" offers it — `src/app/zinseszins-rechner/ZinseszinsCalculator.tsx`.
- After gaining the Sparrate and Dauer modes the calculator described only one third of itself: the text under the heading, the meta description, the JSON-LD and the home-page card all still said it computes how a Sparplan grows, with no hint it also solves for the rate or the horizon a goal needs — rewrote the registry entry to cover all three questions and added keywords for the two new ones; because nav, home page, sitemap, JSON-LD and the 404 listing all derive from that single entry, the one edit updated five surfaces, and the answer stays above the fold at 320, 390 and 1280 px despite the longer text — `src/lib/tools.ts`.
- A printed projection carried two controls that do nothing on paper — "Link kopieren" and "Mit dieser Dauer weiterrechnen" — so a sheet meant for an advisor read as a screenshot of an app; hid the action row with `print:hidden`, leaving the answer, the assumptions, the chart and the full table intact. (A companion rule to make the chart fill the page width was written and then reverted: Recharts writes the width it measured on screen onto the SVG, and forcing `width: 100%` collapsed the wrapper to 0 px, printing an empty box where the chart had been — verified in the PDF, so the chart keeps its slightly narrow screen width for now.) — `src/app/zinseszins-rechner/ZinseszinsCalculator.tsx`.
- The "suite" was one calculator after twenty-four iterations of polish, so the portal spine had nothing to prove itself against — added three more tools: a Vorabpauschale-Rechner (§ 18 InvStG) ported from `EntnahmeplanSuite`'s C# TaxEngine together with all ten of its test vectors, carrying over `MidpointRounding.ToEven` because the vectors only reproduce to the cent with the C# rounding mode; a Kredit- und Tilgungsrechner for Annuitätendarlehen with full Tilgungsplan, Restschuld at the end of the Zinsbindung and a refusal when the instalment cannot cover the first month's interest; and a Kaufen-oder-Mieten comparison in which whichever side pays less per month invests the difference, so renting is not flattered or penalised by ignoring the capital it frees. Home page now populates three of four categories — `src/lib/engine/tax/*.ts`, `src/lib/engine/finance/annuitaet.ts`, `src/lib/engine/finance/kaufen-mieten.ts`, `src/app/{vorabpauschale-rechner,kreditrechner,kaufen-oder-mieten}/`, `src/lib/tools.ts`.
- The Einkommen category was empty and its flagship was blocked: a Brutto-Netto-Rechner needs the year's Sozialversicherung figures, which must not be shipped from memory — sourced them instead from the primary publisher (GKV-Spitzenverband, "Rechengrößen und Grenzwerte im Versicherungs- und Beitragsrecht für das Jahr 2026", 1. Januar 2026) and built a Sozialabgaben-Rechner on them: all four branches with employee and employer shares, both Beitragsbemessungsgrenzen, the childless Pflege surcharge borne by the employee alone and the 0,25 % discount for the second through fifth child. Verified by hand — 4.000 € brutto gives 846 € employee share, capped at 1.508,92 € above the ceilings. Constants live in a per-year file citing the source; the Lohnsteuer half still needs the official Programmablaufplan and is not guessed at — `src/lib/engine/tax/constants/2026.ts`, `src/lib/engine/tax/sozialversicherung.ts`, `src/app/sozialabgaben-rechner/`.
- Three of the five calculators lost everything on reload: Vorabpauschale, Sozialabgaben and Kaufen-oder-Mieten kept no scenario in the URL, so eleven fields of a buy-versus-rent comparison vanished on a refresh and could not be shared at all, while the two older calculators kept theirs — added a shared `useScenarioUrl` hook carrying the lesson that cost a bug the first time (writing is gated until the incoming query string has been applied, or the mount-time write overwrites a shared link with the defaults) and wired all three pages to it, reading query values canonically rather than as German input; all five now survive a reload and reproduce for a second reader, and an incomplete form still publishes nothing — `src/lib/useScenarioUrl.ts`, `src/app/{vorabpauschale-rechner,sozialabgaben-rechner,kaufen-oder-mieten}/`.
- The suite could show how capital is built up but not how it is drawn down, so the obvious follow-up question to a Sparplan — "wie lange trägt mich das?" — had no answer anywhere — added an Entnahmeplan-Rechner with both directions: how long a given monthly withdrawal lasts, and the largest withdrawal a horizon can sustain, with the withdrawal rising yearly by inflation so the income keeps its purchasing power and the capital never allowed to go negative. Verified against arithmetic that needs no model (240.000 € at 1.000 €/Monat without return or inflation is exactly 20 Jahre, 0 Monate) and cross-checked between modes: the solved 2.104,06 € does carry the full 30 years when entered back as a fixed withdrawal — `src/lib/engine/finance/entnahmeplan.ts`, `src/app/entnahmeplan-rechner/`.
- The Kapitalertragsteuer category answered what is owed on an unrealised Vorabpauschale but not the far commoner question of what a sale actually costs — added an Abgeltungssteuer-Rechner reusing the ported KESt engine: gain, credit for Vorabpauschalen already taxed in earlier years so the same growth is not taxed twice, Teilfreistellung, Sparerpauschbetrag, and the effective rate against the raw gain (17,1 % on an Aktienfonds versus the nominal 26,375 %), with a loss stated as untaxed rather than shown as a zero. Verified end to end: 50.000 € sale on a 30.000 € cost gives 3.428,75 €, and crediting 2.000 € of Vorabpauschalen drops it to 3.059,50 €. Also fixed the Rechenweg printing "-0,00 €" for a zero deduction — `src/lib/engine/tax/abgeltungssteuer.ts`, `src/app/abgeltungssteuer-rechner/`.
- The header listed every calculator, so the portal spine got worse with each one added: seven tools wrapped onto four rows and ate 35 % of a 320 px phone screen, and the answer card sat only 30 px above the fold there — one more calculator would have pushed it under, making the loop's own job the thing that broke the pages. Switched the nav to the four categories, which stays four entries however many tools exist, and built the category landing pages the outline had scoped but never got, driven by the same registry. Header on a 320 px phone fell from 197 px to 113 px and the answer gained 84 px of headroom; home → Kategorie → Rechner verified end to end — `src/app/layout.tsx`, `src/app/kategorie/[id]/page.tsx`.
- Nothing in the suite answered the question people actually ask about a portfolio they already hold — what return did it achieve — and the obvious do-it-yourself answer is wrong whenever money was paid in along the way, because comparing the start value to the end value credits the contributions to the return — added a Rendite-Rechner that bisects the same projection the Sparplanrechner uses, giving a money-weighted return, and shows the naive start-to-end figure beside it so the gap is visible: 10.000 € plus 200 €/Monat ending at 45.000 € is 4,29 % a year, not the 16,23 % the naive comparison suggests. Verified against the closed form where one exists (a doubling over ten years with no contributions gives 7,18 %) and by feeding the solved rate back through the projection — `src/lib/engine/finance/rendite.ts`, `src/app/rendite-rechner/`.
- The Kredit-Kategorie asked for a Kaufpreis and a flat Nebenkosten-Prozentsatz, leaving the reader to guess the one number that is actually fixed by law and varies by a factor of nearly two across Germany — added a Kaufnebenkosten-Rechner with the Grunderwerbsteuer of all sixteen Bundesländer, Notar/Grundbuch and Maklerprovision as editable conventions rather than pretend constants, and the point of the whole thing: banks lend against the property and not the fees, so it shows what is left of the equity afterwards and flags when it does not cover them at all (30.000 € against 48.280 € of costs leaves -18.280 € and a full-price financing need). Rates were taken only where two independent published tables agreed on all sixteen values; a search summary claiming Thüringen at 6,5 % contradicted both and was discarded — `src/lib/engine/tax/grunderwerbsteuer.ts`, `src/lib/engine/finance/kaufnebenkosten.ts`, `src/app/kaufnebenkosten-rechner/`.
- The property tools covered buying to live in but not buying to let, so the number every Exposé quotes — the Bruttomietrendite — had nothing to check it against — added a Mietrendite-Rechner that puts both yields side by side: gross divides rent by the asking price, net divides rent-less-costs by everything actually invested, and on the default case that is 4,00 % against 2,50 %. It also reports the monthly cashflow after the costs a landlord cannot pass on and the loan instalment (-473,33 € on 80.000 € equity), which is the figure that decides whether a rental is affordable rather than merely profitable, and states plainly that tax on rental income is not modelled — `src/lib/engine/finance/mietrendite.ts`, `src/app/mietrendite-rechner/`.
- The Einkommen category had a single tool because its flagship was blocked on the amtlicher Programmablaufplan — but that document is only needed for monthly Lohnsteuer, while the assessed Einkommensteuer is a compact formula in the law itself — added an Einkommensteuer-Rechner implementing § 32a EStG in its version "ab dem Veranlagungszeitraum 2026", taken from gesetze-im-internet.de: all five zones with their statutory coefficients and the prescribed rounding, Grund- und Splittingtarif, Kirchensteuer, and Solidaritätszuschlag with the Milderungszone cap of 11,9 % of the Unterschiedsbetrag confirmed from § 4 SolZG rather than recalled. Spot-checks reconcile by hand: 60.000 € einzeln 14.233 €, dieselben 60.000 € mit Splitting 8.434 € = 2 × Tarif(30.000) — `src/lib/engine/tax/einkommensteuer.ts`, `src/app/einkommensteuer-rechner/`.
- Two pages still scrolled sideways when text was enlarged — the home page by 99 px and the Sozialabgaben-Rechner by 73 px — and the culprit was not the heading fixed earlier but the body copy: German compounds such as "Beitragsbemessungsgrenzen" have no break opportunity, so any paragraph containing one widened the page. Since the whole document is German and `<html lang="de">` is already set, hyphenation now applies at the root rather than element by element; all 13 pages are free of horizontal scrolling at 200 % text, currency figures stay unbroken and the line count at normal width is unchanged — `src/app/globals.css`.
- Ten of the eleven calculators carried a shareable scenario in the URL with nothing on the page saying so, and those URLs run to 113 characters — not something anyone picks out of a phone's address bar — so the sharing built over several iterations was effectively invisible everywhere except the one calculator that had a button — extracted that button into a `CopyLinkButton` and put it on all eleven, moving the original page onto the shared component too rather than leaving two implementations to drift; the component keeps the properties that mattered (hidden in print, confirmation announced via `aria-live`, and cleared the moment the scenario changes so it cannot outlive the link it confirmed) and accepts the Zinseszins hand-over actions as children. All eleven verified: clipboard matches the address bar and a second reader opening the link sees the same figure — `src/components/CopyLinkButton.tsx`, all `src/app/*/`.
- The suite could project a Sparplan and it could tax a Vorabpauschale or a sale, but nothing put the two together, so the question the German ETF audience actually asks — what is left after tax — had no answer: doing only one of the two taxes overstates the result — added an ETF-Sparplan-nach-Steuern that taxes each year's Vorabpauschale during the accumulation phase and then the sale, crediting the already-taxed amounts against the gain so the same growth is not taxed twice (10.000 € plus 300 €/Monat at 7 % over 20 Jahre: 190.957,84 € brutto, 174.653,84 € netto, 15,0 % des Gewinns). Its gross figure matches the Zinseszinsrechner to the cent, and every assumption that would otherwise be hidden is stated on the page — the Basiszins is only published to 2026 and is carried forward beyond it, and the Vorabpauschale tax is debited from the settlement account rather than the portfolio — `src/lib/engine/finance/netto-sparplan.ts`, `src/app/etf-sparplan-nach-steuern/`.
- With twelve calculators every one was an island: a page's content contained no links at all, so a multi-step task like buying a flat meant going back through the header and the category page between each step, and a reader on the Sparplanrechner had no way to learn that the after-tax version existed — added the related-tool links the outline had scoped but never got, driven by the registry: each calculator declares the tools that answer the question it raises next, deliberately across categories (a Sparplan leads to its taxation, a purchase to its financing), with same-category tools filling any remaining slots so nothing is ever left empty. Rendered by `CalculatorShell`, so all twelve gained them at once; the Kaufnebenkosten → Kredit → Kaufen-oder-Mieten chain is now one click per step, with no self-references and no change to mobile layout — `src/lib/tools.ts`, `src/components/CalculatorShell.tsx`.
- Nothing in the suite covered inheritance or gifts, one of the larger sums a German household ever deals with — added an Erbschaft- und Schenkungsteuer-Rechner built from the statute (§ 16 Abs. 1 for the Freibeträge, § 19 Abs. 1 for the rate table, § 19 Abs. 3 for the Härteausgleich, all from gesetze-im-internet.de). The Härteausgleich is implemented rather than waved away because this is a Vollstaffel — the whole taxable amount is charged at the band's rate — so one euro over a boundary would otherwise cost thousands: at 75.001 € steuerpflichtig the raw band rate gives 8.250,11 € while the statute caps it at 5.250,50 €, fifty cents more than the euro below. Same 475.000 € costs a child 5.250 € and a stranger 136.500 €, which is the point of the Steuerklassen — `src/lib/engine/tax/erbschaftsteuer.ts`, `src/app/erbschaftsteuer-rechner/`.
- The Geldanlage tools could build capital, draw it down and measure its return, but nothing measured what inflation quietly removes — added an Inflationsrechner that gives both directions, which are routinely confused and are not the same number: 10.000 € at 2 % over 20 Jahre is worth 6.729,71 €, but 14.859,47 € would be needed to buy what it buys today. It also reports the Halbwertszeit (35 Jahre bei 2 %). Its discounting agrees to the cent with the "Kaufkraft heute" column the Sparplanrechner already showed. Deflation surfaced a wording bug in my own page — "-22,3 % weniger" — now reading "22,3 % mehr" with the figure relabelled Kaufkraftgewinn — `src/lib/engine/finance/inflation.ts`, `src/app/inflationsrechner/`.
- The Einkommen category was the thin one at two tools, and its remaining flagships stay blocked — added a Stundenlohn-Rechner, which needs no external constants at all, and made it earn its place by giving the two rates that differ: dividing a 48.000 € salary by contracted hours gives 23,08 €, but only 1.760 of those 2.080 hours are actually worked once 30 Urlaubstage and 10 Feiertage are paid but not worked, so the rate for time at the desk is 27,27 € and the paid days off are worth 8.726,40 €. Both figures reconcile by hand. Fixed a misstatement in my own copy along the way: "23,08 € — 18,2 % weniger" framed a surcharge on the nominal rate as a reduction from the effective one, where the honest reading of that figure is 18,2 % more (the reduction would be 15,4 %) — `src/lib/engine/finance/arbeitszeit.ts`, `src/app/stundenlohn-rechner/`.
- The Kredit-Kategorie covered property but not the other big financed purchase — added a Leasing-oder-Kaufen-Rechner that counts both sides whole: leasing looks cheaper per month because nothing is being bought, while buying ends with a car still worth something (40.000 € bei 15 % Wertverlust sind nach vier Jahren 20.880,25 €), and whichever side ties up less money earns a return on the difference. Also added `annuitaetFuerLaufzeit`, since consumer loans fix the term rather than the Tilgungssatz. Caught a contradiction before it shipped: with the car paid in cash the card declared Leasing günstiger while showing a lower figure for Kauf, because the verdict was computed on effective cost including that return while the displayed figures excluded it — the card now shows what the verdict compares, and the table exposes the step (bar bezahlt: Zwischensumme 20.800 € gegen 19.119,75 €, aber 4.755,15 € Rendite auf das nicht gebundene Kapital) — `src/lib/engine/finance/leasing-kauf.ts`, `src/lib/engine/finance/annuitaet.ts`, `src/app/leasing-oder-kaufen/`.
- A portal exists to be found, and this one was invisible to search: `/sitemap.xml` and `/robots.txt` both returned 404, and every canonical was relative (`href="/mietrendite-rechner/"`), which search engines treat as ambiguous — added a registry-driven sitemap and robots.txt, so a new calculator appears in both without anyone remembering, and set `metadataBase` so the canonicals resolve absolute. The sitemap carries 21 entries (Startseite, vier Kategorien, sechzehn Rechner) with none missing. The domain is still an open question in the outline, so it is not baked in: `NEXT_PUBLIC_SITE_URL` sets it at build time and the fallback is an obvious `.example` placeholder, since a wrong real domain in a sitemap is worse than none — `src/app/sitemap.ts`, `src/app/robots.ts`, `src/app/layout.tsx`, `src/lib/tools.ts`
- At sixteen calculators the home page had become 4,3 Bildschirme on a phone with no way to search, so answering "was zahle ich beim ETF-Verkauf an Steuern?" meant scrolling two screens and already knowing the answer lives under Kapitalertragsteuer — added a search that filters on the registry's keywords, not just the titles, since someone asking about "ETF verkaufen" is looking for a Rechner whose name they would have to know in advance; the unfiltered list is what renders on the server, so all sixteen stay in the static HTML for crawlers and readers without JavaScript. Two realistic queries came back empty in testing and were fixed as part of the same change — the common misspelling "Erbschaftssteuer" and the colloquial "Hypothek" — along with other words people actually type ("Gehalt umrechnen", "Rente", "vermieten", "Aktien verkaufen"). The target calculator moved from y=1331 to y=463 — `src/components/ToolFinder.tsx`, `src/app/page.tsx`, `src/lib/tools.ts`
- The 404 page listed every calculator, which was sensible when it was written and there was one of them, and had become four screens of tiles on a phone at sixteen — someone who mistypes a URL wants a way out, not a catalogue. Replaced the tool list with the four categories, which stay four however many calculators exist, plus a link to the start page where the new search does the rest; the page fell from 3.344 px to 1.004 px on a phone and now fits a single desktop screen. Same failure mode as the header nav a few iterations earlier: a list that grows with the suite, sitting somewhere that should be a quick exit — `src/app/not-found.tsx`
- Most German mortgage contracts allow an annual Sondertilgung and nothing showed what one is worth; the outline had put it out of scope for v1 rather than out of scope entirely — added a Sondertilgungs-Rechner that runs both scenarios rather than reasoning about the difference, because the instalment stays where the contract set it while every extra euro stops paying interest for the whole remaining term: 5.000 € im Jahr auf 300.000 € bei 3,5 % sparen 63.148,94 € Zinsen und machen neun Jahre und sieben Monate früher schuldenfrei, ohne dass sich die Rate von 1.375 € ändert. It agrees with the Kreditrechner on the untouched loan (29 Jahre, dieselbe Rate), and the saving per euro rises with the interest rate — 0,66 € bei 3,5 %, 1,03 € bei 6 % — `src/lib/engine/finance/sondertilgung.ts`, `src/app/sondertilgung-rechner/`
- Einkommen was the thin category and its two flagships stay blocked, but one high-value question was answerable with the § 32a engine already built — added an Abfindungsrechner implementing the Fünftelregelung from § 34 Abs. 1 EStG, taken from gesetze-im-internet.de: five times the difference between the tax on the ordinary income and the tax on that income plus a fifth of the severance, including Satz 3 for the case where there is no ordinary income at all. It cross-checks against the Einkommensteuer-Rechner to the cent (10.548 € bei 50.000 €, 18.264 € bei 70.000 €, macht 5 × 7.716 = 38.580 €), and the relief behaves as the rule intends: 23.014 € gespart ohne laufendes Einkommen, exakt 0 € für jemanden, der ohnehin im Spitzensteuersatz liegt — `src/lib/engine/tax/abfindung.ts`, `src/lib/engine/tax/einkommensteuer.ts`, `src/app/abfindung-rechner/`
- Error text used Tailwind's `red-500`, which sits at 3,81:1 on white — below the 4,5:1 AA threshold — so every "Bitte eine Zahl eingeben" across eighteen calculators, and every negative figure on Mietrendite und Kaufnebenkosten, was under-contrasted in the default light theme; dark mode scraped through at 4,55:1. Replaced it with an `--error` token picked per theme, giving 6,64:1 hell und 8,54:1 dunkel. Two of my own measurements were wrong before this one was right: `getComputedStyle` returns Tailwind 4 colours as `lab()`, which my contrast function first parsed as RGB and then failed to normalise through a canvas, producing a confident 1,83:1 that pointed at the wrong theme entirely — the real defect was in light mode, not dark — `src/app/globals.css`, `src/components/NumberField.tsx`, `src/app/mietrendite-rechner/`, `src/app/kaufnebenkosten-rechner/`
- The Sozialabgaben-Rechner assumes employment with an employer paying half, which leaves the self-employed with no answer at all — added a Krankenversicherung-für-Selbstständige-Rechner on the same sourced 2026 figures, with the two things that make it different: the whole rate falls on one person, and the contribution never drops below the statutory Mindestbemessungsgrundlage, so 500 € Gewinn still costs 278,17 € — 55,6 % of it. It reconciles with the employee calculator (4.000 € ergibt 844 €, genau dessen KV 700 + PV 144 und genau das Doppelte des Arbeitnehmeranteils von 422 €), handles the reduced rate without Krankengeld and the cap above the Beitragsbemessungsgrenze, and states that Rentenversicherung is not included — `src/lib/engine/tax/selbststaendige-kv.ts`, `src/lib/engine/tax/constants/2026.ts`, `src/app/selbststaendige-krankenversicherung/`
- A full sweep of all nineteen calculators found one regression: the Abgeltungssteuer-Rechner dropped its "Link kopieren" button whenever the sale was a loss, because the share control sat inside the branch that renders the profit summary. A loss is a valid, computed result with a valid URL — 0,00 € Steuer auf 10.000 € Verlust — and was the one state in the whole suite that could not be shared. Moved the button so it appears in both branches; verified the clipboard matches the address bar and a second reader opening the link sees the same result — `src/app/abgeltungssteuer-rechner/AbgeltungssteuerRechner.tsx`
- The category names were written for four calculators and had quietly stopped describing their contents: "Kapitalertragsteuer" had come to include Erbschaft- und Schenkungsteuer, which is not one, so anyone scanning for inheritance tax would skip the heading; "Kredit — Annuitätendarlehen, Tilgungsplan und Effektivzins" held four tools needing no loan at all, Mietrendite among them; and "Einkommen" advertised Lohnsteuer, the one calculator deliberately not built. Renamed to "Steuern auf Vermögen" and "Immobilien und Kredit" and rewrote all four descriptions to match what is actually there. The ids drive the URLs and were left untouched, so the four Kategorie-Adressen, the sitemap and every existing link still resolve — `src/lib/tools.ts`
- The site's own one-line description had drifted the same way the category names had: "Rechner für Geldanlage, Steuern und Kredit" named three of four areas and silently omitted Einkommen, by then five calculators — and that string is the default `<title>` on every page. The meta description still advertised "Kapitalertragsteuer", a category renamed the iteration before. Both now name all four areas, and the README inventory was regenerated from the registry: it had listed twelve of nineteen Rechner, and my first attempt at regenerating it produced seven because the parser assumed a fixed field order that most entries do not follow — `src/lib/tools.ts`, `src/app/layout.tsx`, `README.md`
- The suite could price a loan but not answer the question every buyer asks first — added "Wie viel Haus kann ich mir leisten?", which runs the financing backwards from the monthly rate and gets the part people get wrong: the Kaufnebenkosten cannot be borrowed, so the affordable price is not Eigenkapital plus Darlehen but what remains once the fees have taken their share of both. 1.500 €/Monat und 100.000 € Eigenkapital tragen in NRW 381.255,22 €, in Bayern 391.741,75 € — zehntausend Euro mehr Haus für dasselbe Geld, allein wegen der Grunderwerbsteuer. The inverse reconciles with the Kreditrechner (327.273 € Darlehen ergibt dort exakt die vorgegebenen 1.500 €), Preis plus Nebenkosten deckt sich auf den Cent mit Darlehen plus Eigenkapital, and equity too small to cover the fees is named rather than silently absorbed — `src/lib/engine/finance/budget-immobilie.ts`, `src/app/immobilien-budget/`
- Added a Kirchensteuer-Rechner, which answers the question the headline rate obscures: 9 % sounds like 9 % of income but is 9 % of the *Einkommensteuer*, and church tax is deductible as a Sonderausgabe, so part comes back. Bei 60.000 € zvE sind das 1.280,97 € im Jahr, netto etwa 786,77 € — 1,31 % des Einkommens, nicht 9 %. The gross figure reconciles to the cent with the Einkommensteuer-Rechner; the relief is explicitly labelled an estimate on the page, because the deduction lands in the assessment of the year it was paid and a large one walks down the progression — `src/lib/engine/tax/kirchensteuer-last.ts`, `src/app/kirchensteuer-rechner/`
- Twenty-one calculators shared no visual system beyond a border colour: every card was the same literal `rounded-xl border border-border bg-surface` repeated across 21 files, so nothing could be restyled without editing all of them, and the palette had exactly one accent with no elevation, no motion and no hover state anywhere — replaced the ad-hoc utilities with a token layer and a small set of component classes (`.card`, `.card-link`, `.btn`, `.field-input`, `.segmented`, `.chip`, `.badge`, `.data-table`) written as plain CSS against the tokens, then swept the 21 calculators onto them; a colour is now decided in one place for both themes and a restyle no longer means a 21-file diff — `src/app/globals.css`, all `src/app/*/`.
- The theme was whatever the operating system said and could not be overridden: a reader on a light desktop who wanted the dark palette had no way to ask for it, and there was nothing in the chrome that suggested a dark palette existed — added a three-state toggle (System/Hell/Dunkel) that writes `data-theme` on `<html>` and remembers the choice, with the stored value read through `useSyncExternalStore` rather than a mount effect and applied by a pre-paint inline script so a dark reader never gets a white flash on load; the CSS resolves `[data-theme]` over `prefers-color-scheme` in both directions, so an explicit "hell" survives a dark OS — `src/components/ThemeToggle.tsx`, `src/app/globals.css`, `src/app/layout.tsx`.
- Getting from one calculator to another meant leaving the one you were on: the header held four category links, the search lived only on the start page, and the path between any two of 21 tools was home → search → click — added a ⌘K/Strg-K command palette (also "/", also a header button for anyone who knows neither) that searches the same keywords the start page does and opens the match with Enter; it is a combobox over a listbox with `aria-activedescendant`, so arrowing through the results announces each calculator instead of silently moving a highlight, and the search ranking now puts a title hit above one buried in the keyword list — `src/components/SiteSearch.tsx`, `src/lib/search.ts`, `src/components/SiteHeader.tsx`.
- On desktop the answer scrolled away from the inputs: the form sat in a grid row of its own, so on a Tilgungsplan of 40 rows the reader changed a rate at the top of the page and then had to scroll back up to see what it did — the loop these calculators exist for, broken on every one of them. The panel is now sticky beside the results, which needed the grid fixing first: `grid-row: 1 / -1` refers to the last line of the *explicit* grid and there was none, so it silently collapsed to a single row and the panel's own height inflated row 1 — a 355 px gap under every result card. Spanning a fixed large row count with zero row-gap and margins for the spacing gives the panel somewhere to travel; a form taller than the viewport scrolls inside itself. The Zinseszins page went from 2.990 px to 2.234 px with no content removed — `src/app/globals.css`, all `src/app/*/`.
- The same loop was worse on a phone, where the form is taller than the viewport: the result card is deliberately first, so changing an input meant scrolling down to type and back up to read — added a bar at the bottom edge that mirrors the headline figure and its label as soon as the card scrolls out of view, and gets out of the way again when it returns. It reads the card through three `data-` attributes rather than taking a prop, because the figure is already rendered in 21 calculators that each format and label it differently, and it is `inert` while hidden so it never turns up in the tab order behind the page — `src/components/StickyResult.tsx`, `src/components/CalculatorShell.tsx`, all `src/app/*/`.
- A year-by-year table ran to 40 rows and pushed the related calculators, the footer and everything else off the end of the page, and its header row scrolled away long before the last row — capped the table at 32 rem with the header row sticky inside it, and lifted the cap for print, where a scroll container would otherwise print as a cropped table (verified against the print stylesheet: all 20 rows, no chrome, no buttons) — `src/app/globals.css`, all `src/app/*/`.
- The unit inside a number field was positioned over the input with the padding guessed at, so "20" in the Anlagedauer field rendered as "2C" behind "Jahre" — the value was literally unreadable on the field that decides the horizon. The unit is now a flex sibling that takes the width it needs while the input gets the rest, so no label is too long to fit, and it is referenced from `aria-describedby` rather than left silent, which is what a screen reader needs to tell years from months — `src/components/NumberField.tsx`, `src/app/globals.css`.
- The start page was 21 identical text blocks under four headings, with the full multi-clause description on every card, and the only way to narrow them was to type — gave each card the promise clause and dropped the rest to the calculator's own page, added a category mark, an arrow that moves on hover and a Rechtsstand badge, and put filter chips with live counts beside the search for readers who are browsing rather than looking; the calculator pages gained breadcrumbs and the footer gained a full index, so no page is a dead end — `src/components/ToolFinder.tsx`, `src/app/page.tsx`, `src/app/layout.tsx`, `src/components/CalculatorShell.tsx`, `src/app/kategorie/[id]/page.tsx`, `src/app/not-found.tsx`.
- The suite could say how capital grows and how long it lasts but not the question those two feed — "how far am I from not having to work" — and the FIRE vocabulary people ask it in (Coast, Barista, Lean, Fat) had nothing to check it against. Added a FIRE-Rechner that treats each stage as its own condition on today's capital rather than as a threshold on one scale (Barista can need more than Lean when the side income is small), reports the highest rung reached, and for every rung the capital it needs, the share already there and the age it arrives at with the current savings. Coast is the moving one: its number rises towards the full FIRE number as the Ruhestand approaches, so the projection compares each month's balance against that month's need. Everything is in real terms, and the compounding is the Sparplanrechner's exactly (pinned by a test), so 100.000 € at 5 % over two years is 110.250 € in both. Hand checks: 36.000 € at 3,5 % is 1.028.571,43 €; 100 % over one year halves the Coast number to 514.285,72 €; 420 € a year at 0 % takes exactly twelve months of 1.000 € — `src/lib/engine/finance/fire.ts`, `src/app/fire-rechner/`, `tests/finance/fire.test.ts`, `src/lib/tools.ts`.
